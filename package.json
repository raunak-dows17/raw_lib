{
  "name": "raw_lib",
  "version": "1.0.4",
  "description": "RawLib — RawQL engine, adapters and validation module",
  "license": "MIT",
  "author": "Raunak pndey",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsup src/index.ts --format cjs,esm --dts --out-dir dist --clean --sourcemap",
    "build:watch": "tsup src/index.ts --format cjs,esm --dts --out-dir dist --clean --sourcemap --watch",
    "prepare": "npm run build",
    "publish": "npm publish --access public"
  },
  "dependencies": {
    "mongoose": "^8.18.0",
    "tsx": "^4.20.5"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.3.3",
    "jest": "^30.1.3",
    "mongodb-memory-server": "^10.2.0",
    "ts-jest": "^29.4.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  },
  "keywords": [
    "rawql",
    "rawlib",
    "clean-architecture",
    "validation",
    "graphql"
  ]
}
